<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bancomr">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BancoMR">
        <activity android:name=".investimento"></activity>
        <activity android:name=".emp" />
        <activity
            android:name=".setting"
            android:label="@string/title_activity_setting" />
        <activity android:name=".poupanca" />
        <activity android:name=".transf" />
        <activity android:name=".faturas" />
        <activity android:name=".Saldo" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>